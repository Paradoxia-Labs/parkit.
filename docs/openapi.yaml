openapi: 3.0.3
info:
  title: Parkit API
  version: 0.1.0
  description: "OpenAPI spec placeholder for Parkit API. Add paths and components here."
servers:
  - url: http://localhost:3000
paths: {}
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
